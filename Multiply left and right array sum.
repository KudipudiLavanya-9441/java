class Complete{
    
   
    // Function for finding maximum and value pair
    public static int multiply (int arr[], int n) {
        //Complete the function
        int left_array_sum = 0;
        int right_array_sum = 0;
        
        for( int i = 0; i<n/2 ;i++){
            left_array_sum = left_array_sum + arr[i]; 
        }
        
        for(int i = n/2; i<n; i++){
            right_array_sum = right_array_sum + arr[i];
        }
        
        return left_array_sum*right_array_sum;
    }
    
    
}

output:
For Input:
4
1 2 3 4
your output is: 
21
